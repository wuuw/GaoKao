'use strict';
/**
 * model:major, 自动对应数据库表格 ref_major
 */
export default class extends think.model.base {
  //查询所有
  // selectAll(sql_1, sql_2, field, order, sort, page) {
  //   return this.where(sql_1).where(sql_2 || null).field(field).order(order + ' ' + sort).page(page).countSelect();
  // }
}
