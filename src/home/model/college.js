'use strict';
/**
 * model college, 自动对应数据库表格 ref_college
 */
export default class extends think.model.base {
  //查询所有
  // selectAndJoin(sql_1, sql_2, joinTable, on, field, order, sort, page) {
  //
  // }
}
